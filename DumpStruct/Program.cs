using System; using Lumina; using Lumina.Excel.Sheets; class Program { static void Main() { var gameData = new GameData("C:\\Program Files (x86)\\SquareEnix\\FINAL FANTASY XIV - A Realm Reborn\\game\\sqpack"); var sheet = gameData.GetExcelSheet<ClassJob>(); foreach(var row in sheet) { if(row.Abbreviation.ToString() == "BLM" || row.Abbreviation.ToString() == "PLD") Console.WriteLine($"{row.Abbreviation}: Role={row.Role}"); } } }
