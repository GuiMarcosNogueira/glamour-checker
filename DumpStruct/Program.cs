using System; using Lumina; using Lumina.Excel.Sheets; class Program { static void Main() { var gameData = new GameData("C:\\Users\\Illidan\\AppData\\Roaming\\XIVLauncher\\addon\\Hooks\\dev"); var sheet = gameData.GetExcelSheet<ClassJob>(); foreach(var job in new[] { "PLD", "WAR", "WHM", "SCH", "MNK", "BRD", "BLM" }) { foreach(var row in sheet) { if (row.Abbreviation.ToString() == job) Console.WriteLine($"{job} - Role: {row.Role}"); } } } }
